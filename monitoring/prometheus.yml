###############################################################################
# WMS Panama — Prometheus Configuration
###############################################################################

global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    system: wms-panama
    environment: production

alerting:
  alertmanagers:
    - static_configs:
        - targets: []

rule_files:
  - "alerts/*.yml"

scrape_configs:
  # FastAPI backend (prometheus-fastapi-instrumentator)
  - job_name: wms-api
    static_configs:
      - targets: [api:8000]
    metrics_path: /metrics

  # PostgreSQL
  - job_name: postgres
    static_configs:
      - targets: [postgres-exporter:9187]

  # Redis
  - job_name: redis
    static_configs:
      - targets: [redis-exporter:9121]

  # Nginx
  - job_name: nginx
    static_configs:
      - targets: [nginx-exporter:9113]

  # Celery (flower)
  - job_name: celery
    static_configs:
      - targets: [flower:5555]
    metrics_path: /metrics
